# Python
__pycache__/
*.pyc
.venv/
venv/
.env
# Data & weights (large, not committed)
pipeline/weights/*
!pipeline/weights/.gitkeep
!pipeline/weights/README.md
pipeline/data/raw/*
pipeline/data/tiles/*
!pipeline/data/**/.gitkeep
# keep sample output committed? no
pipeline/data/output/*
!pipeline/data/output/.gitkeep
# Node
web/node_modules/
web/dist/
web/.vite/
# OS
.DS_Store

# pipeline data cache
pipeline/data/cache/*
!pipeline/data/cache/.gitkeep
pipeline/vendor_xview2_1st/

# web build
web/dist/

# The demo dataset (tiles + contract GeoJSON + event registry + live snapshots)
# IS committed so the app runs on a fresh clone with no pipeline run. Regenerate
# with pipeline/scripts/{run.py,make_tiles.py} + web/scripts/refresh_live_snapshots.mjs.
# Raw Maxar COGs and pipeline scratch stay ignored (rules above).
